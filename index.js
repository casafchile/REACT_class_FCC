const JSX = (
  <div className="myDiv">   //clase en REACT es con el atributo className en vez de class de html
                            //y para usarlo se puede usar onClick onChange 
    <h1>Add a class to this div</h1>
  </div>
);
